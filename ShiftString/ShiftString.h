#pragma once
#include <vector>
#include <string>
using namespace std;
class ShiftString
{
public:
    ShiftString(string& input, string& target);
private:
    int solution(string&  input, string& target);
};
